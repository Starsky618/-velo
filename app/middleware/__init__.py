"""
中间件 / 跨切面工具模块——"楼里所有房间都用得上的公共设施"。

这个目录放的不是某个具体业务模块的代码，而是**横向**为多个模块提供服务的工具：
限流、CORS、请求日志、错误统一处理、安全头注入等等。

为什么单独开一个目录：
    类比小区物业。每户人家不会自己装电梯、自己烧热水——这些是公共设施。
    放一起便于统一维护，也避免每个业务模块自己造轮子（比如限流逻辑散在 5 处略有不同）。

输入/输出数据流：
    入：上层 router 函数调用工具函数（如 check_rate_limit_by_user(...)）
    出：抛 HTTPException（限流触发）/ 返 None（放行）
    边界：本目录代码**不直接操作业务表**（不读 Activity / User），只读 Redis / 飞书 webhook 等基础设施

操作注意：
    - 新增工具时优先评估"是不是真的横切多模块"——只服务 1 个模块的应该回到该模块内
    - 避免循环依赖：本目录不允许 import 任何业务模块（app/user / app/activity 等）
"""
