# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
venv/
.venv/

# 环境变量（含密钥，绝对不能提交）
.env

# 上传的文件
uploads/

# IDE
.vscode/
.idea/
*.swp

# 微信开发者工具本机私有配置：每台电脑的开关不一样，不应该跟着代码库发布。
miniprogram/project.private.config.json

# OS
.DS_Store
Thumbs.db

# Docker
caddy_data/
caddy_config/

# DB 备份目录（Sprint 5 task-1 / 生产 host 路径 ~/velo/backups/）
backups/

# Superpowers 工具内部文件
.superpowers/
docker-compose.override.yml

# Claude Code 本地权限配置（本机专属 allow 列表，不跨机共享）
.claude/settings.local.json

# Claude Code superpowers worktree 临时目录（本机会话产物，不进版本库）
.claude/worktrees/
.claude/agent-weekly/
docs/agent-weekly/

# 本机采集 / 导出 / 调研产物（2026-07-25 盘点补：合计 ~300MB，无一个已跟踪文件）
# 这些目录从 2026-06 起一直挂在 git status 里，把真实代码改动埋在噪音底下。
# 确有某个文件要入库时用 git add -f 单独指定。
/outputs/
/output/
/exports/
/prototypes/
/tmp_doc_work/
/design-qa.md

# playwright-cli 本机调试快照与 console 日志
.playwright-cli/

# 原型工程依赖（route-atlas 自带 .gitignore，这条兜住其他原型）
docs/prototypes/**/node_modules/
