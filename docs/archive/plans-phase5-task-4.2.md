# 任务 4.2：黑盒度三问体检

## 🎯 目标

按 CLAUDE.md "防黑盒化"硬要求，每期收尾必答三问，验证：
1. 10 分钟讲全貌 —— 项目整体架构能 10 min 内讲清
2. 数据流复述 —— 任意一条核心链路能不查文档复述
3. 30 秒读懂任意文件 —— 任意 Python 文件 / __init__.py 顶部一句话能让新人秒懂这个文件干啥

## ⛓ 前置依赖

task-4.1（架构 / 数据流文档已刷新）。

## 📤 输出契约

| 产出 | 用途 |
|---|---|
| 三问体检结论（写入 changelog 或单独 docs/blackbox-check-v5.md） | 验收 |
| 不达标项清单 | 后续修补 |

## 🛠 操作步骤

### 1. 第一问：10 分钟讲全貌

主 agent 给自己计时 10 分钟，对着 architecture-guide.md 讲：
- v5 完成了什么主轴（B / C / A / D）
- 项目当前 8 模块 + 共享 common 层的边界 / 依赖方向
- 数据库 7 张表的关系 + v5 新建 2 张
- 核心反馈环：用户骑车 → GPX → Worker → 匹配 → 排行榜 → 通知 / 分享 → 用户激励
- 关键运行时（worker / monitor / scheduler / curation-pool-cron 4 容器）

不能讲清的点 → architecture-guide.md 该处文字补强。

### 2. 第二问：数据流复述

不查文档，对 7 条 v5 新增链路 + 9 条 v0-v4 既有链路，每条复述：
- 用户做什么动作
- 哪些模块被触发
- 数据从哪到哪
- 关键陷阱 / 幂等点

复述卡壳的链路 → data-flow-guide.md 该链路文字补强。

### 3. 第三问：30 秒读懂任意文件

随机抽 5 个 Python 文件 / 文件夹 __init__.py，不读上下文只看顶部，要求 30 秒内回答：
- 这个文件 / 文件夹是干什么的（生活类比）
- 改它有什么坑

不达标 → 在该文件顶部补一句话说明（CLAUDE.md "文件可读性要求"硬要求）。

## ✅ 验收

写入 `docs/changelog.md` v5 段尾：

```markdown
### 黑盒度三问体检（task 4.2）

- [x] 10 分钟讲全貌 —— 通过 / N 处补强（列文件:行号）
- [x] 数据流 16 条复述 —— 通过 / N 条补强
- [x] 30 秒读懂任意文件 —— 抽 5 个 / N 个补一句话说明
```

## 📝 commit

```
chore(docs): 任务 4.2 黑盒度三问体检

- 第一问通过：architecture-guide.md 全貌可 10 min 讲完
- 第二问通过：16 条数据流（v5 新增 7 + v0-v4 既有 9）可不查文档复述
- 第三问通过：抽样 5 文件全 30 秒可懂
- N 处补强（详见 commit diff）
```

## 🔍 自检三问

1. **诚实标准**：自我体检能"放水"吗？  
   → 不能。讲不清就明确标"不达标"补强，不假装通过。CLAUDE.md "稳扎稳打有疑必停"原则。

2. **新人视角**：30 秒读懂的标准是"非项目成员"能懂，不是"我自己写的当然懂"。  
   → 模拟"刚加入项目的工程师"思维做检查。

3. **不达标项处理**：要在 v5 期内修完吗？  
   → 是。CLAUDE.md "防黑盒化"硬要求"任何一项不满意当期清完，不留下期"。
