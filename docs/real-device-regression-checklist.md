# velo 真机回归清单（2026-06-13 上线前一次性走查）

> **怎么用**：微信开发者工具重新编译 → 真机扫码预览 → 照下面顺序逐条点一遍。
> 任何一条不对，截图/描述给 Claude，当场修。**这一趟跑通即达上线点。**
>
> **前提**：生产后端已是最新（`c94b786` + 迁移 `20260612_meetup_power_hints`）；
> 本轮纯前端改动 `89954458` 在本地工作区，编译即生效，无需部署服务器。
> 真机连的是生产域名 `https://api.weiluai.top`（HTTPS 已通）。

---

## A. 这几天修的 bug —— 验证真的修好了（最高优先）

这批是"静态测试全绿但真机用不了"的，必须真人确认：

- [ok] **A1 路线轨迹图显形**：打开任一路线详情（如天龙山），手风琴展开"怎么骑"，看到橙色纸面轨迹缩略图（不是白屏）
- [不行，加载很慢，然后完全加载不出来（白屏）也不能打开放大] **A2 个人页热力图**：进"我的"页，骑行热力图卡画出轨迹线（不是空白/不是腾讯默认底图）
- [ok] **A3 活动列表轨迹缩略图**：活动列表里每条骑行左边有小轨迹图
- [ok] **A4 约骑列表卡轨迹**：约骑列表里卡顶有路线轨迹缩略
- [没卡死，但是没有微信一键登录] **A5 退出登录回得了"我的"页**：设置页点退出登录 → 跳回"我的"页显示"微信一键登录"（不是卡死在设置页）
- [ok] **A6 首页"去登录"按钮**：未登录态首页点"去登录" → 跳到"我的"页（不是点了没反应）

---

## B. 集合点搜索 & 组件化 —— 本轮新交互首次真机

- [联想搜索只能显示太原的，而不是全国任意地区。并且我又有新想法：这种搜索只能显示地点名，但倘若我想用更细节的名字当起终点名（如南中环桥东自行车道）现有技术完全弄不了，我在想怎么办] **B1 实时联想搜索**：发起约骑 → 集合地点整行点进去 → 输入框打"晋祠"，候选实时蹦出一列（不是只有一个结果/不是要点按钮）
- [拖图不卡，但是无法地图点击选点，变成只能关键词搜索了] **B2 拖图不卡**：在地图上按住拖动，流畅（不卡顿）
- [看不懂，没法做] **B3 选候选地图跳转**：点列表里某个候选，地图跳过去、针尖对准
- [看不懂，没法做] **B4 确认回填**：点"确认集合点"，回到编辑页，集合地点那行显示选中的地名
- [ok] **B5 功率/均速滚轮**：确认页"节奏与门槛"，推荐功率/预计均速是滚轮挑区间（不是手打框）；选"高强度拉练"自动跳到对应档
- [ok] **B6 门槛标签多选**：确认页点"设置门槛"，弹出一排标签可多选，选中橙色高亮，拼成一句显示
- [ok] **B7 补给点快选**：发过一次约骑后，第二次发起时补给点输入框下面出现"最近用过"快选
- [ok，但路线规划算法很差，很多不是骑行专用道] **B8 起终点搜索生成路线**：用搜索选起点+终点生成一条路线，看轨迹贴路（不偏移）

---

## C. 发起约骑完整链路 —— 端到端真走一遍

- [ok] **C1 选路线**：发起约骑 → 选一条有轨迹的路线（清徐夜骑/天龙山/启春阁/横岭/狼坡/奥申任一）
- [ok] **C2 编辑详情**：填时间、集合点、人数、节奏、补给点、说明
- [ok] **C3 加照片**：传 1-2 张照片
- [ok] **C4 确认页**：适合谁标签、可见范围、门槛、安全提示、骑友栏都正常
- [ok] **C5 发布成功**：点发布 → 跳到约骑详情页
- [没有具体参与者，没有分享按钮，地图只显示轨迹不显示底图] **C6 详情页完整**：路线轨迹卡、数据、参与者、分享按钮都在

---

## D. 半生人加入约骑 —— 用另一个微信号（社交闭环核心）

- [没多号试不了] **D1 公开约骑**：发一个"本城可见"约骑 → 另一个号在约骑列表能看到 → 点进去能加入
- [没多号试不了] **D2 私圈约骑**：发一个"私圈可见"约骑 → 微信转发分享链接 → 另一个号点链接（带口令）能看到详情并加入；没口令的人看不到（404）
- [没多号试不了] **D3 加入后参与者列表**：加入后，发起人详情页参与者列表出现这个号的昵称/头像

---

## E. 战报闭环 —— 骑完点亮格子（激励源核心）

- [ok] **E1 FIT/GPX 真传一次**：约骑当天，上传一个骑行文件，解析成功
- [没试过，不知道怎么触发] **E2 战报格子点亮**：约骑战报里你的格子从灰变亮，显示距离/数据。
      ⏱ **attach tick 每 5 分钟跑一次**（生产已确认在调度），上传完骑行文件后最多等 5 分钟格子才亮——这是正常的，别误判 bug
- [没试过] **E3 跨午夜夜骑（D2 修订验证）**：如方便，测一个晚出发的约骑——出发后几小时内动身的骑行也能挂上格子（不再因跨午夜变灰）
- [没试过，不知道怎么触发] **E4 战报"交卷"跳转**：战报页点"交卷" → 跳到上传页且带约骑横幅（本轮修的 tabBar 跳转）

---

## F. 路线百科实景图 —— 上一批 ship 的验收

- [ok] **F1 实景图长廊**：路线详情页数据卡下面有实景图横滑长廊（崛围山 5 张/汾河 7 张/玉泉山 8 张/天龙山 3 张）
- [ok] **F2 点开全屏**：点任一实景图，全屏看大图、左右翻、长按可保存
- [ok] **F3 hero 封面渐变**：封面图底部一小段淡入页面底色，标题坐在淡出区

---

## 已知不在本轮范围（看到不用报）

- 狼坡/奥申实景图 0 张（缺图挂账，你拍了照片扔文件夹即可）
- 5 条路线无轨迹（环汾河/崛围山/庙前山/小西沟/玉泉山 = track_pending，地图/约骑按钮不显示，正常）
- 玉泉山/横岭/崛围山媒体图待换 velo 自拍
- 水印分享功能（形态已定，未实现）
