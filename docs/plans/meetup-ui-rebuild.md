# 约骑模块 UI 全面重构 + profile 结构重排 · 任务卡

> 立卡：2026-06-12（Tim 拍："整个约骑模块都要进行重构，目前字体大小、排版都不合格，还要嵌入地图轨迹模块"）
> 设计真相源：`miniprogram/design-system/MASTER.md` v0.4（苹果方案：HIG + 系统橙，中文字重 ≤600，数字 tabular-nums）
> 执行纪律：起手必重新 grep 本卡"现状"全部断言（task 卡 grep 数据必 stale 教训）；每页改完跑 wxml↔wxss↔js 三层 class/函数对照自校验；质感 gate = Tim 真机，不是编译通过。

## ⚠ 真机验收链路（2026-06-12 实证教训，验收前必读）

**改完代码 ≠ Tim 手机能看到。** 本地改动只在开发者工具模拟器即时生效；Tim 手机上的"体验版"是上次**上传**的老包。实证：2026-06-12 四项改动（常显数据卡/约骑白卡/图表色/字重）Tim 真机"全部感受不到"，原因就是没走上传。正确链路：改完 → 开发者工具**预览**（扫码，本地代码真机跑）给 Tim 看 → 过审后**上传**新体验版。汇报时必须说清当前处于哪一环。

## Tim 报的问题（原话，验收时逐条对）

1. "约骑所有详情页都没有进行改动"——2026-06-12 已做 meetup-detail.wxss 白卡化（`911706a6`，class 对照已验证生效）；**执行时先让 Tim 重新编译确认基线**，若仍觉得没变=他要的是结构级重排，不是换肤
2. "有些字体真机看特别小"——全模块字号审计：正文 ≥28rpx、辅助 ≥24rpx、任何 20/22rpx 必须有存在理由
3. "编辑页面地图与按键位置冲突" bug——meetup-create 的地图区与按钮重叠/遮挡，**需真机复现定位**（疑似 map 组件原生层级盖住普通元素：小程序 map 是原生组件，z-index 高于一切非 cover-view 元素——按钮若浮在地图上必须用 cover-view 或移出地图区域）
4. "排版不合格"——对齐 MASTER：卡 28rpx 圆角、行间 hairline、大留白、inset-grouped 列表语言（参照已过审的 route-detail/explore/home）
5. "嵌入地图轨迹模块"——约骑卡/详情页要有地图轨迹可视（参照 Tim 朋友 Codex 首页图的地图卡形态）

## 范围（5 页 + 数据链路）

| 页面 | wxss 行数 | 已做 | 待做 |
|---|---|---|---|
| meetup-create（编辑/创建） | 1472 | 字重/最小字号 sed | **重头戏**：位置冲突 bug / 字号排版全审 / 步骤流（选路线→编辑→确认）布局重排 |
| meetup-detail（详情） | 222 | wxss 白卡化重写 | 结构级：地图轨迹卡前置常显、信息层级重排 |
| meetups-list（列表 tab） | 119 | 安全手术 | 卡片重排 + **地图轨迹缩略嵌入**（标杆：Tim 朋友图的地图卡） |
| meetups-mine（我的约骑） | 131 | 仅旧色清理 | 对齐列表页新语言 |
| meetup-report（战报） | 218 | 仅旧底色 | 全面对齐（它是分享场景，参照成绩卡水印哲学） |
| map-picker（选点） | 120 | 仅旧底色 | 复查（map 原生组件 + 自定义控件的 cover-view 纪律） |
| **profile（我的，追加）** | 576 | 字重手术 | Tim："本身也丑"——结构级重排：头部用户卡/数据汇总区/功能列表全部按 inset-grouped 语言重做（参照已过审 home/route-detail） |

## 地图轨迹嵌入·数据链路（执行时 grep 验证）

- meetup-detail 已有 `route-preview-map`（map 组件 + polyline，route_book 关联时显示）——确认 meetups-list 卡片可复用同数据源（meetup API 是否返 preview_points？不返则后端 schema 增量——**先查 spec 再动后端，Tim 授权边界"别动后端 API 端口字段"以 2026-06-12 对话为准，新增字段需 Tim 拍**）
- 列表卡地图缩略方案二选一（执行时给 Tim 真机对比）：a) 真 map 组件（多实例性能风险，列表 10+ 卡=10+ 原生 map，可能卡）b) 静态轨迹线 canvas/分享图（性能好，无交互）——**倾向 b**，列表只要"形状感"，点进详情才要真地图
- 底图统一走 `utils/map-theme.js`（subkey 已配，layerStyle 纸面风）

## 不许做

- 不动 meetup 后端（service/router/schemas）除非 Tim 拍字段增量
- 不改约骑业务流程（步骤顺序、字段、口令门禁逻辑）——只动皮和布局
- 不引入新设计语言——一切从 MASTER v0.4 派生，禁止任何深底霓虹/装饰回潮

## 验收

- Tim 真机逐页过（编译 → 走完整发起约骑流程 + 看详情 + 看战报）
- 他报的 3 个具体问题（字体小/位置冲突/无轨迹）逐条销账
- pytest 全绿（不动后端时应零影响）+ 前端三层对照自校验记录
