<view class="map-picker-page">
  <view class="map-picker-map-shell">
    <map id="picker-map"
         class="map-picker-map"
         latitude="{{latitude}}"
         longitude="{{longitude}}"
         scale="14"
         enable-traffic="{{false}}"
         enable-3D="{{false}}"
         enable-rotate="{{false}}"
         show-location="{{false}}"
         bindregionchange="onRegionChange">
      <cover-view class="map-picker-pin">
        <cover-view class="map-picker-pin-core"></cover-view>
      </cover-view>
    </map>
  </view>

  <!-- 唯一操作路径（Tim 拍）：输入关键字实时联想 → 点候选地图跳过去 → 微调针尖 → 确认。
       不提供手填名称——名字跟着选中的地点走，骑友挑现成的不打字。 -->
  <view class="map-picker-panel">
    <view class="picker-title">{{title}}</view>
    <view class="picker-search">
      <input class="picker-search-input" value="{{searchKeyword}}" maxlength="40" bindinput="onSearchKeywordInput" confirm-type="search" placeholder="输入地名实时搜索，例如：晋祠公园" />
    </view>
    <view class="picker-search-results" wx:if="{{placeSearchResults.length}}">
      <view class="picker-search-result tappable" wx:for="{{placeSearchResults}}" wx:key="title" data-index="{{index}}" bindtap="onTapSearchResult">
        <view class="picker-result-title">{{item.title}}</view>
        <view class="picker-result-address">{{item.address}}</view>
      </view>
    </view>
    <view wx:if="{{name}}" class="picker-picked">已选：{{name}}</view>
    <view class="picker-actions">
      <button class="picker-cancel" bindtap="onTapCancel">取消</button>
      <button class="picker-confirm" bindtap="selectMapPoint">{{confirmText}}</button>
    </view>
  </view>
</view>
