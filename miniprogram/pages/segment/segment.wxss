/* task-4.5 step 1 占位样式 / step 2-7 实施时替换为完整 4 区块 */

.placeholder {
  padding: 80rpx 40rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24rpx;
}

.seg-id {
  font-size: 36rpx;
  color: var(--color-primary, #FF2D55);
  font-weight: 600;
}

.hint {
  font-size: 28rpx;
  color: #86868B;
}
