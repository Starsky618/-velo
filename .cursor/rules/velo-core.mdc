---
description: VELO 常驻入口——以根 AGENTS.md 为唯一硬加载规则
alwaysApply: true
---

# VELO Cursor 对齐（最小）

**唯一常驻入口**：根目录 `AGENTS.md`。本规则只强制加载纪律，不重复其全文。

## 必守

1. 先读会改的代码与直接相关合同；不要默认全文加载 `CLAUDE.md`、workflow kernel、分工宪章、旧 task 卡。
2. 信息优先级：当前用户要求 > 真实运行/代码 > 当期合同 > 其他文档与记忆。
3. 只改任务范围；不清理、不回滚、不覆盖工作区里其他人的改动。
4. 不自动 stage / commit / push / 部署；这些动作需 Tim 明确授权。
5. “完成”必须带：改了什么、证据是什么、哪些真实路径未验证。

## 与 Codex 的边界

- 共享：`AGENTS.md`、`docs/`、`.agents/skills/`（按需触发）。
- 不共享：`~/.codex/memories/`。跨 agent 共识只进 git 文档，不靠私有 memory。
- `CLAUDE.md` = 技术陷阱/历史细则检索库，不是常驻全文。
