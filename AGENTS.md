# VELO 项目不变量

这里只保留无法从代码稳定推断、但每次违反都可能改变产品或制造假完成的事实。通用协作与交付规则以全局 `AGENTS.md` 为准。

## 1. 产品边界

- VELO 服务中国城市里的严肃公路车骑手，核心是骑行身份、真实户外数据和本地社交。
- 不扩成综合运动平台，不自制硬件或做装备电商，不做实时导航/动态改路，不做付费解锁或室内虚拟骑行；路线功能提供静态路线、GPX 和外部地图跳转。
- 产品取舍大体保持社交 60%、工具 40%；新增工具要说明它怎样帮助骑手交流、发现路线或形成身份。
- 只有遇到新功能、商业化或目标用户变化时，才读取 `docs/agent-rules/product-decisions.md` 并让 Tim 决定产品取舍。

## 2. 真实信息优先级

- 当前用户要求 > 真实运行结果与代码 > 当前明确引用的合同/任务说明 > 其他文档与记忆。
- 旧 spec、plans、`docs/superpowers/` 和工作流文档只算历史证词，除非当前任务明确引用，不能当成现状或授权。
- 只读取本任务直接相关的代码与文档；找文档时用 `docs/README.md`，部署时才读 `docs/agent-rules/deploy-sop.md`。其他历史工作流只有在当前任务明确点名时才加载。

## 3. 高风险边界

- 新功能默认不向 `users`、`activities`、`segments`、`segment_efforts` 塞字段；确需修改时，必须说明迁移、兼容和未来删除代价。
- 新增或修改环境变量、接口字段、状态值和数据库结构时，以真实定义为准，并同步代码、配置、迁移、测试和部署要求。Alembic revision id 不超过 32 个字符。
- 长期脏工作区属于正常环境；只碰任务范围，绝不顺手清理、回滚或覆盖其他改动。

## 4. 验收边界

- 本地没有可信的 VELO 业务数据库。SQLite/mock 只作快速反馈；PostgreSQL/PostGIS、Redis 和迁移由 GitHub CI 的临时空白服务验证；生产状态只能在获得相应授权后到服务器核实。
- CI 缺少依赖或静默跳过不算通过。
- 微信小程序的页面、地图、canvas、手势、授权和跳转必须在微信开发者工具或真机验证；静态测试不能证明用户已经看到。
- 外部服务的 mock 只证明代码会调用；上线前后至少真实触发一次关键路径。提交、CI、部署和用户真实可用是四层不同证据，汇报时不得混写。
