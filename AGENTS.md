# VELO 最小操作规则

目标：让 agent 在 VELO 中知道产品不能变成什么、哪些动作风险最高、怎样证明工作真的完成。通用推理和编码方法交给模型；专项手册只在对应任务出现时读取。

## 1. 产品方向

VELO 服务中国城市里的严肃公路车骑手，核心是骑行身份、真实户外数据和本地社交。涉及产品方向时遵守：

- 只做公路车垂直；不扩成综合运动平台。
- 不自制硬件，也不做装备电商。
- 不做实时导航或路径重规划；提供静态路线、GPX 和外部地图跳转。
- 不做付费解锁功能，也不做室内虚拟骑行。
- 产品取舍大体保持社交 60%、工具 40%；新工具功能要说明它怎样帮助骑手继续交流、发现路线或形成身份。
- 完整产品规则只在新功能、商业化或用户范围决策时读 `docs/agent-rules/product-decisions.md`。

## 2. 真实信息的优先级

当前用户要求 > 真实运行结果与代码 > 当期产品合同或任务说明 > 其他文档与记忆。旧 spec、plans、`docs/superpowers/` 和工作流文档都是历史证据，除非当前任务明确引用，不能自动当成现状。

- 不再为每个任务固定读取 `CLAUDE.md`、`docs/README.md`、workflow kernel、分工宪章和旧 task 卡；先读本次会改的代码与直接相关合同。
- `CLAUDE.md` 保留详细技术陷阱和历史规则，遇到对应技术或事故再搜索相关段落，不默认全文加载。
- 找文档时用 `docs/README.md`；多 agent 协作或正式交接时才读 `docs/agent-rules/agent-collaboration.md`。

## 3. 代码边界

- 后端在 `app/`，迁移在 `migrations/versions/`，测试在 `tests/`，微信小程序在 `miniprogram/`。
- 新功能优先放新模块或新表，避免把 `users`、`activities`、`segments`、`segment_efforts` 继续变成杂物间；确需修改时说明删除、迁移和兼容代价。
- 依赖大体沿 `User ← Activity ← Segment ← Notification ← Strava ← RouteBook ← Meetup`。新增反向依赖必须显式说明原因和替代方案；不要复制历史例外。
- 新增环境变量、接口字段或数据库结构时，代码、配置、迁移、测试和部署说明必须同步；名字与状态值以真实定义为准。
- 工作区长期可能很脏。只改任务范围内文件，不清理、不回滚、不覆盖其他改动。

## 4. 选最小工作方式

- 目标、范围和验收清楚：直接读代码、实现、验证，不先造 spec 或任务卡。
- bug 或性能问题：先做一个能稳定复现用户症状的检查，再定位和修复；没有真实信号时不要靠猜。
- 产品行为、交互手感或架构关键决定未定：先看真实样本或参照；看见后更容易判断时，先做可删除的最小 HTML / 交互原型，和 Tim 高频真看真改。样本未验证前不扩成持久架构或完整全栈实现。
- 多小时任务只有在一个终点需要反复试验时才启用 Goal；多个可独立验收的交付物再拆任务。任务小不是失败，能快速到达真实结果更好。
- 已能客观定义的行为、bug 和数据不变量，优先先写失败检查再实现；视觉、产品形态和真实设备手感用原型与真用反馈判断，不能让单测代替。双 agent、spec、ADR 或 Matt 全流程仍只在能解决明确风险时启用。
- 部署前才读取 `docs/agent-rules/deploy-sop.md`。commit 不等于上线；没有明确授权不得 push、改服务器或发布生产。

## 5. 验收

- 后端默认：先跑受影响测试，再按风险跑 `pytest tests/ -q`。本地 SQLite / mock 只提供快速反馈，不能冒充真实服务证据。
- VELO 当前可作为真实状态依据的数据库都在服务器，本地没有有效业务数据库。数据库结构和 PostgreSQL / Redis 集成测试在 GitHub CI 的临时空白服务中验收；生产状态只在明确授权后到服务器核实。CI 测试缺少服务或静默跳过都不能算通过；确有平台条件差异时应拆成明确 job 或补等价证据，不能用 skip 假绿。Alembic revision id 不超过 32 个字符。
- 微信小程序：静态测试只能证明文件和契约存在。页面显示、地图、canvas、手势、授权和跳转必须在微信开发者工具或真机验证，不能把 mock 通过说成用户已经看见。
- 外部服务：mock 只证明代码会调用。上线前后至少真实触发一次关键路径，确认 webhook、队列、OAuth 或通知真的到达。
- 本地提交门是 `scripts/pre_commit_gate.sh`；远端兜底是 `.github/workflows/test.yml`。门禁通过不替代产品真用。

## 6. 审查强度

- 普通、局部、测试能直接抓错的改动：实现者读 diff + 目标测试即可。
- 跨模块契约、测试盲区、调用链遗漏或大改：加一道独立集成审查，审查者必须读真实 diff 和调用方。
- schema/迁移、鉴权、隐私、不可逆数据、并发竞态：必须有一道独立审查，并补上与风险对应的真实证据——迁移要跑新数据库，鉴权和隐私要测越权路径，并发要测重试或重复执行。只有风险同时跨越多个边界或 Tim 明确要求时，才增加第二位审查者。
- 审查发现按 `Critical / Important / Minor` 报告；阻断项必须给 `file:line`、出事场景和最小修法。没有新问题就直接写没有，不虚构。

## 7. 完成边界

“完成”至少说明：改了什么、证据是什么、哪些真实路径没验证。不要自动 stage、commit、push 或部署；这些动作由 Tim 明确授权。
