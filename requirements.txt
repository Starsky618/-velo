# VELO v1 核心依赖
# 版本锁定于 2026-04-14，保证构建可复现

fastapi==0.135.2
uvicorn==0.42.0
sqlalchemy==2.0.49
psycopg2-binary==2.9.11
alembic==1.18.4
pydantic-settings==2.13.1
PyJWT==2.12.1
httpx==0.28.1
rq==2.7.0
redis==7.4.0
gpxpy==1.6.2
geoalchemy2==0.18.4
python-multipart==0.0.24

# 第 1 期新增：翻译层
garmin-fit-sdk>=21.200.0       # FIT 文件解析（Garmin 官方库）
xyconvert>=0.1.2               # GCJ-02→WGS84 坐标转换（依赖 numpy）
numpy>=1.24.0                  # xyconvert 的依赖

# 第 2 期新增：Strava 集成
rq-scheduler>=0.13.1           # RQ 定时任务调度（调度器用）

# 测试依赖
pytest==9.0.2
